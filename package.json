{
  "name": "vergeroofing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:pages": "node scripts/build.js",
    "migrate": "node db/migrate.js",
    "test": "node --test --test-concurrency=1 --experimental-test-module-mocks 'test/**/*.test.js'",
    "lint:dashes": "node scripts/check-dashes.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "@node-rs/argon2": "^2.0.2",
    "@vercel/blob": "^0.27.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "pg": "^8.13.1"
  }
}
